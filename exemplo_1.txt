() >= int ! main {
    int x;
    int i;
    x = 3;
    i = 0;
    while (i < 100) {
        if (x == 3) {
            return 3;
        };
        if (x == 5) {
            return 5;
        };
        i = i + 1;
    };
    return 0;
}
