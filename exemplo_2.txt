int n;

() >= int ! main {
    n = 10;

    int a;
    a = 0;

    int i;
    i = 0;

    while (i < n | a < 10) {
        {
            if (i % 2 == 0) {
                a = a + 1;
            } else {
                a = (3 * a) / 2 * i;
            };
        };
        i = i + 1;
    };

    a = a + 1;
    return a;
}
