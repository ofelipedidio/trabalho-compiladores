() >= int ! main {
    int n;
    n = 10;

    int a;
    a = 0;

    int i;
    i = 0;

    while (i < n) {
        {
            if (i % 2 == 0) {
                a = a + 1;
            } else {
                a = (3 * a) / 2;
            };
        };
        i = i + 1;
    };

    a = a + 1;
    return a;
}
